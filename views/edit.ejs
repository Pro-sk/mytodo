<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Blog</title>
    <link rel="stylesheet" href='../assets/style/blog-form.css'>
    <base href="/">
    <style>
        form{
            width:50%;
            padding: 20px;
            box-sizing: border-box;
            margin:20px auto;
            box-shadow: 0px 0px 20px rgba(0,0,0,0.1);
        }
        form input[type=text],textarea
        {
            padding:10px;
            width:100%;
            font-family: sans-serif;
            font-weight: bold;
            box-sizing: border-box;
            display: block;
            caret-color: blueviolet;
            margin:10px auto;
            border:1px solid #76497c;
        }
        form .btn{
            padding:10px 25px;
            border:1px solid #26aac2;
            color:#fff;
            background-color: aqua;
            cursor: pointer;
            transition:all 0.5s;
            font-weight: bold;
        }
        form .btn:hover{
            transform:scale(1.1);
            border-radius:10px;
        }
        @media screen and (max-width:900px) and (min-width:260px)
        {
            form{
                width: 95%;
            }
        }
    </style>
</head>
<body>
    <%- include("./partials/nav.ejs") %>
    <form action="/post/edit" method="post">
        <input type="hidden" name="id" value="<%= data.id  %>">
        <input type="text" name="title" id="title" placeholder="Title Here" value='<%= data.title %>'>
        <textarea name="desc" id="desc" rows="20" placeholder="Description Here.."><%= data.desc %></textarea>
        <button type="submit" class="btn">Update</button>
        <a href="/post/<%= data.id  %>" class="btn">Back</a>
    </form>
</body>
</html>